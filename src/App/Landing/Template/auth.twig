  
{% extends _layout %}

{% block content %}
   <br><br><br><br>
    <h1>Please authorise</h1>
    {% if(level > 0) %}
    {% if(level == 2) %}
    <h2>Welcome admin menu {{ login }}</h2>
     <p>
      <form action="" method="post">
       <input type="text" name="login" value="{{ login }}"><br>
       <input type="submit" value="Ban">
      </form>
     </p>
    {% else %}
      <h2>Welcome {{ login }}</h2>
    {% endif %}
    {% else %}
    <p>
     <form action="" method="post">
      <input type="text" name="login" value="{{ login }}"><br>
      <input type="password" name="pass"><br>
      <input type="submit" value="Sigin in">
     </form>
    </p>
    {% endif %}

{% endblock %}